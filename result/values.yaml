replicaCount: 1

image:
  repository: suryaprasad9773/votingapp
  tag: results
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: docker-pwd

service:
  type: ClusterIP
  port: 80
  targetPort: 80
